---
title: 'Schedule'
description: 'Add image, video, and other HTML elements'
icon: 'calendar'
---

<img
  style={{ borderRadius: '0.5rem' }}
  src="https://mintlify-assets.b-cdn.net/bigbend.jpg"
/>

## Add a class

### Single Class

To add a single class go to the specific day that you want the class to take place. Once you found the date click the `Add class` button. After you click `Add class` you will see a pop up with five feilds:

<ResponseField name="Class type" type="text" required>
Name of your project. Used for the global title.

Example: `Weightlifting for beginners`

</ResponseField>

<ResponseField name="Trainer" type="option">
Name of your project. Used for the global title.

Example: `Arnold Schwarzenegger`

</ResponseField>

<ResponseField name="Max capicty" type="number">
Name of your project. Used for the global title.

Example: `20`

</ResponseField>

<ResponseField name="Time" type="time" required>
Name of your project. Used for the global title.

Example: `10:00 am`

</ResponseField>

<ResponseField name="Duration" type="option" required>
Name of your project. Used for the global title.

Example: `1 hour`

</ResponseField>

<ResponseField name="Reacurring class" type="option">
Name of your project. Used for the global title.

Example: `Monday, Tuesday, Friday`

</ResponseField>


## Remove a class

### Single Class

The [markdown syntax](https://www.markdownguide.org/basic-syntax/#images) lets you add images using the following code

### Reccuring Class

The [markdown syntax](https://www.markdownguide.org/basic-syntax/#images) lets you add images using the following code